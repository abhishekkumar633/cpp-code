#include<stdio.h>
int main()
{
   int n,sum=0;
   int arr[100];
   printf("enter the the size of array\n");
   scanf("%d",&n);
   printf("enter the elemnt of array\n");
   for(int i=0;i<n;i++)
   scanf("%d",arr[i]);
   printf(" the elemnts of array are:\n");
   for(int i=0;i<n;i++)
   printf("elements are ",arr[i]);
   for(int i=0;i<n;i++)
    {
        int sum=0;
        sum++;
    }
    return sum;
    printf("Sum is %d",sum);

return 0;

   }


